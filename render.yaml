# render.yaml - Blueprint para despliegue automático en Render
# Documentación: https://render.com/docs/blueprint-spec

services:
  - type: web
    name: mano-poderosa-frontend
    env: static
    region: oregon
    plan: free
    rootDir: frontend
    buildCommand: npm install && npm run build
    staticPublishPath: dist
